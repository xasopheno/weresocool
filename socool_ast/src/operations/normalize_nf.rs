pub mod normalize_nf {
    extern crate num_rational;
    extern crate rand;

    use crate::ast::{Op, OpTable, OscType, OpOrNf};
    use crate::operations::{GetLengthRatio, NormalForm, Normalize};
    use num_rational::Ratio;

}

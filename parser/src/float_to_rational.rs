extern crate num_rational;

pub mod helpers {
    pub fn f32_string_to_int(f: String) {

    }

    #[cfg(test)]
    pub mod tests {
        use super::*;

     #[test]
        fn test_float_to_int() {
            let result = false;
            let expected = true;
            assert_eq!(result, expected);
        }
    }
}

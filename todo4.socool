struct VoicesManager {
    pub voices: [Option<Voices>; 2],
    read_idx: usize,
}

struct RenderManager {
    pub buffers: [Option<StereoWaveform>; 2],
    write_idx: usize,
    read_idx: usize,
}



struct Renders {
    RenderVoices: RenderVoices,
}

